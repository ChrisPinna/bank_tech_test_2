const BankInterface = require('../../src/bankInterface')

describe('Bank Program', () => {
 test('it should be able to make a deposit and get a bank statement with transaction in it', () => {
  const bank = new BankInterface;
 });
});